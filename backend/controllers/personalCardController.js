const PersonalCard = require("../models/personalCard.model");exports.getAllPersonalCards = async (req, res, next) => {    try {        const personalCards = await PersonalCard.find();        res.status(200).json({message: 'All personal cards were fetched successfully', personalCards});    } catch (error) {        if (!error.statusCode) {            error.status = 500;        }        next(error);    }};exports.getPersonalCards = async (req, res, next) => {    const id = req.params.id;    try {        const personalCards = await PersonalCard.findById(id).exec();        if (!personalCards) {            let error = new Error('Not found!');            error.statusCode = 404;            throw error;        }        res.status(200).json({message: 'Personal card item has been successfully fetched!', personalCards});    } catch (err) {        if (!err.statusCode) {            err.statusCode = 500        }        next(err)    }};exports.deletePersonalCards = async (req, res, next) => {    const id = req.params.id;    try {        const personalCards = await PersonalCard.findByIdAndRemove(id);        if (!personalCards) {            let error = new Error('Not found!');            error.statusCode = 404;            throw error;        }        res.status(200).json({message: 'Personal card has been successfully deleted!', personalCards});    } catch (err) {        if (!err.statusCode) {            err.statusCode = 500;        }        next(err);    }};exports.addPersonalCards = async (req, res, next) => {    try {        const {            name,            text,            academicDegree,            image,            faculty        } = req.body;        const personalCards = await PersonalCard.create({name, text, academicDegree, image, faculty});        res.status(201).json({message: 'Personal card was successfully created', personalCards});    } catch (error) {        if (!error.statusCode) {            error.statusCode = 500;        }        next(error);    }};exports.editPersonalCards = async (req, res, next) => {    try {        const {            name,            text,            academicDegree,            image,            faculty        } = req.body;        const oldFaculty = await PersonalCard.findByIdAndUpdate(req.params.id, {            name,            text,            academicDegree,            image,            faculty        });        res.status(200).json({message: 'Personal card was successfully updated!', oldFaculty})    }  catch (error) {        if(!error.statusCode) {            error.statusCode = 500        }        next(error)    }};