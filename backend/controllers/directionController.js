const Direction = require("../models/direction.model");exports.getAllDirections = async (req, res, next) => {    try {        const directions = await Direction.find();        res.status(200).json({message: 'All directions fetched successfully!', directions})    } catch (error) {        if(!error.statusCode) {            error.statusCode = 500        }        next(error)    }};exports.getDirection = async (req, res, next) => {    try {        const direction = await Direction.findById(req.params.id);        if (!direction) {            let error = new Error('Not found!');            error.statusCode = 404;            throw error;        };        res.status(200).json({message: 'Direction fetched successfully!', direction});    } catch (error) {        if (!error.statusCode) {            error.statusCode = 500        }        next(error)    }};exports.createDirection = async (req, res, next) => {    try {        const {            name,            abbreviation,            description,            linkToWebSite,            image,            faculty        } = req.body;        const newDirection = new Direction({            name,            abbreviation,            description,            linkToWebSite,            image,            faculty        });        const direction = await Direction.create(newDirection);        res.status(200).json({message: "Direction created successfully!", direction});    }catch (error) {        if(!error.statusCode) {            error.statusCode = 500        }        next(error)    }};exports.deleteDirection = async (req, res, next) => {    try {        const direction = await Direction.findByIdAndDelete(req.params.id);        if (!direction) {            let error = new Error('Not found!');            error.statusCode = 404;            throw error;        }        res.status(200).json({message: 'Direction item has been successfully deleted', direction});    } catch (error) {        if (!error.statusCode) {            error.statusCode = 500        }        next(error)    }};exports.editDirection = async (req, res, next) => {    try {        const {            name,            abbreviation,            description,            linkToWebSite,            image,            faculty        } = req.body;        await Direction.findByIdAndUpdate(req.params.id, {            name,            abbreviation,            description,            linkToWebSite,            image,            faculty        });        const updatedDirection = await Direction.findById(req.params.id);        res.status(200).json({message: "Direction updated successfully!", updatedDirection});    } catch (error) {        if (!error.statusCode) {            error.statusCode = 500        }        next(error)    }};