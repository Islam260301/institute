const router = require('express').Router();let personalCardController = require('../controllers/personalCardController');router.get('/', personalCardController.getAllPersonalCards)router.get('/:id', personalCardController.getPersonalCards)router.delete('/:id', personalCardController.deletePersonalCards)router.post('/', personalCardController.addPersonalCards)router.put('/:id', personalCardController.editPersonalCards)module.exports = router;