import * as a_types from '../actions/actionTypes';const initialState = {    faculties: []}export const facultyReducer = (state = initialState, action) => {    switch (action.type) {        case a_types.GET_ALL_FACULTIES:            return {...state, faculties: action.faculties}        // case a_types.GET_FACULTY:        default:            return state    }}