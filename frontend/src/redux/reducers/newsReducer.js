import * as a_types from '../actions/actionTypes';const initialState = {    news: []}export const newsReducer = (state = initialState, action) => {    switch (action.type) {        case a_types.GET_ALL_NEWS:            return {...state, news: action.news}        default:            return state    }}