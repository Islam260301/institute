import React from 'react';import * as Icon from 'react-bootstrap-icons';import style from './Admin.module.css';import {Button} from "react-bootstrap";export const Item = (props) => {    const title = props.title    const id = props.id    return (        <li key={id}            className={`${style.item} p-2 d-flex justify-content-between align-items-center`}>            <div>                {title}            </div>            <div className={`d-flex gap-lg-3`}>                <Button variant={`info`}><Icon.PencilSquare size={35}/></Button>                <Button variant={`danger`}><Icon.TrashFill size={35}/></Button>            </div>        </li>    )}