import React, {useEffect} from 'react';import {NavLink, useHistory} from "react-router-dom";export const AdminNav = (props) => {    const currentPath = '/adminka';    const navData = [        {            title: 'Events',            link: '/editEvents',        },        {            title: 'News',            link: '/editNews',        },        {            title: 'Faculties',            link: '/editFaculties',        },        {            title: 'Directions',            link: '/editDirections',        },        {            title: 'For applicants',            link: '/editForApplicants',        },        {            title: 'Personal cards',            link: '/editPersonalCards',        },    ]    const history = useHistory();    useEffect(() => {        if (history.location.pathname === currentPath || history.location.pathname === currentPath + '/') {            debugger            history.push(currentPath + navData[0].link)        }    }, [])    return (        <ul className={`nav nav-pills d-flex justify-content-around`}>            {navData[0] ? navData.map(e => {                return (                    <li className={`nav-item`}>                        <NavLink                            className={`nav-link`}                            activeClassName={`active`}                            exact                            to={`${currentPath + e.link}`}>{e.title}</NavLink>                    </li>                )            }) : 'loading...'}        </ul>    )}