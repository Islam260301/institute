import React, {useEffect} from 'react';import {connect} from "react-redux";import {fetchAllForApplicants} from "../../../redux/actions/forApplicatsActions";import * as Icon from 'react-bootstrap-icons';import style from '../Admin.module.css';import {Button} from "react-bootstrap";import {Item} from "../Item";const ForApplicants = (props) => {    useEffect(() => {        props.fetchAllForApplicants()    }, [])    return (        <div>            <ul>                {props.forApplicants ? props.forApplicants.map(item => {                    return <Item                        key={item._id}                        title={item.title}                        id={item._id}                    />                }) : null}            </ul>        </div>    )}const mapStateToProps = (state) => {    return {        forApplicants: state.forApplicants.forApplicants    }}export default connect(mapStateToProps, {fetchAllForApplicants})(ForApplicants)