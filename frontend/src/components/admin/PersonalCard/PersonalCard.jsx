import React, {useEffect} from 'react';import {fetchAllPersonalCards} from "../../../redux/actions/personalCardActions";import {Item} from "../Item";import {connect} from "react-redux";const PersonalCard = (props) => {    useEffect(() => {        props.fetchAllPersonalCards()    }, [])    return(        <div>            {props.personalCards ? props.personalCards.map(item => {                return (                    <Item                    key={item._id}                    title={item.name}                    id={item._id}                />                )}) : null}        </div>    )}const mapStateToProps = (state) => {    return {        personalCards: state.personalCard.personalCards    }}export default connect(mapStateToProps, {fetchAllPersonalCards})(PersonalCard)