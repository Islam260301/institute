import React, {useState} from 'react';import axios from "../utils/axios";export const AddDir = () => {    const [name, setName] = useState('')    const [abbreviation, setAbbreviation] = useState('')    const [linkToWebSite, setLinkToWebSite] = useState('')    const [description, setDescription] = useState('')    const [facultyId, setFacultyId] = useState('')    const nameOnChangeHandler = (e) => {        setName(e.target.value)    }    const abbreviationOnChangeHandler = (e) => {        setAbbreviation(e.target.value)    }    const linkToWebSiteOnChangeHandler = (e) => {        setLinkToWebSite(e.target.value)    }    const facultyIdOnChangeHandler = (e) => {        setFacultyId(e.target.value)    }    const descriptionOnChangeHandler = (e) => {        setDescription(e.target.value)    }    const addDirec = async () => {        try {            return await axios.post('/direction', {name, description, abbreviation, linkToWebSite,faculty: facultyId})        } catch (error) {            console.log(' server error', error)        }    }    return(        <div>            <input                value={name}                placeholder={`name`}                onChange={(e) => nameOnChangeHandler(e)}            />            <input                value={abbreviation}                placeholder={`abbreviation`}                onChange={(e) => abbreviationOnChangeHandler(e)}/>            <input                value={linkToWebSite}                placeholder={`linkToWebSite`}                onChange={(e) => linkToWebSiteOnChangeHandler(e)}/>            <input                value={facultyId}                placeholder={`facultyId`}                onChange={(e) => facultyIdOnChangeHandler(e)}/>            <textarea                value={description}                placeholder={`description`}                onChange={(e) => descriptionOnChangeHandler(e)}/>            <button onClick={addDirec}>ADD</button>        </div>    )}